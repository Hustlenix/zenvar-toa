{
  "name": "zenvar-toa-admin",
  "private": true,
  "version": "1.0.0",
  "description": "Server-side admin tooling for Zenvar TOA-01 auth (member provisioning). Not deployed.",
  "scripts": {
    "provision": "node provision-members.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5"
  }
}
